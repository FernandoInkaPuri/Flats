<h1>Flats</h1>
<h3>Boas vindas ao sistema de locação de apartamentos</h3>
<% if @properties.empty? %>
<h3> Nenhum imóvel disponível </h3>
<% end %>
<% @properties.each do |p|%>
    <p><%= link_to(p.title, property_path(p.id)) %></p>
    <p><%= p.description %></p>
    <p> Quartos: <%=p.rooms %></p>
<% end %>